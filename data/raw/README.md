# Raw data

Store immutable source files locally using source-specific subdirectories. Raw files are ignored by Git.

For every acquisition, record provider, URL or request procedure, download date, reference date, license/access terms, checksum, coverage, CRS, and known limitations in `docs/data_inventory.md`.
